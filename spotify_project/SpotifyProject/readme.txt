To run:
Be in folder and then:
. .venv/bin/activate
export FLASK_APP=app
export FLASK_ENV=development
flask run

Doesn't work anymore: Or to run with "WARNING: This is a development server.", write (note it doesn't update, and have to uncomment ):
python3 app.py